// there are 3 types of variables (data containers) in js
// var, let, const

const constant = 20;
// we have to initialise while declaration of variable
// block scoped, function scoped

let temp;
console.log(temp); // undefined
temp = 20;
// block scoped, function scoped

var v = 100;
// not used anymore, only function scoped
